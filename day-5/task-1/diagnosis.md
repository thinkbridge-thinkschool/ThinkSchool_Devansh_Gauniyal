This trace showed the slow span was the /authors/slow endpoint's own request span, taking 66.07ms and containing 32 total spans, because the code issued one query for the author list and then a separate database round trip for every single author's books -- 31 round trips instead of one. The first query took 7.98ms and the rest settled around 70-90 microseconds each, but the count, not any single duration, is what made it slow. I'd fix it by loading books through an Include join so EF Core fetches everything in one query, cutting 31 round trips down to 1.
