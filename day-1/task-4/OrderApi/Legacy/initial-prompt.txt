Write a deliberately bad OrderController.cs for an ASP.NET Core 10 API. Make it approximately 200-300 lines with one giant async POST /api/orders action. Mix HTTP handling, weak validation, business logic, EF Core database access, and response formatting inline. Use direct OrderDbContext access in the controller, synchronous EF calls inside the async action, exactly four empty catch { } blocks that swallow exceptions, anonymous object responses instead of typed responses, and no tests or separation of concerns. Include realistic legacy-production complexity and subtle bugs, including an off-by-one mistake and possible null dereferences. Return the generated controller as-is and do not refactor it.
